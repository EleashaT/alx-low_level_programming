#include <stdio.h>
#include "main.h"

/**
 * main - convert a binary to a unsigned int.
 * *b: character
 * Return: 0
 */
unsigned int binary_to_uint(const char *b);
{
	char btouint[] = "01010101";
	char *b = btouint;



	if (b[btouint] != '0' && b[btouint] != '1' && b[btouint] == NULL)
	{
		_putchar("%d\n", '0');
	}

	return (0);

}
